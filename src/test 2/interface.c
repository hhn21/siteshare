#include <stdio.h>
#include <stdlib.h>
#include <stdlib.h>

#include "account.h"
#include "location.h"

typedef enum option{
    ADD,
    SHARE,
    SAVE,
    RESTORE,
    FETCH,
    LOGIN,
    LOGOUT,
    SIGNUP
}option;

int menu(option type,)

void main()
{
    /* code */
    printf("%d\n", a);
    return;
}